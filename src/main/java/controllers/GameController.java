package controllers;

import ninja.Result;
import ninja.Results;

import javax.naming.Context;

/**
 * Created by Zen on 2015/01/23.
 */
public class GameController {

    /*public Result index(){
        Result result = Results.html();
        result.render("Hand", pokerService.dealHand().toString());
        return result;
    }

    public Result hostGame(Context context){

    }

    public boolean joinGame(String username, int gameID){

    }

    public boolean startGame(int gameId){
        Optional<Game> gameOpt = gameRepository.findById(gameID);
        if(!gameOpt.isPresent()) {return Results.notFound();}

        Result result = Results.html();
    }*/
}
